// No pude hacer que funcione el código separando la carpeta de funciones del código que llama a la función

/*let usuario = prompt("Ingrese su usuario");
    usuario = usuario.toLowerCase();
let contrasenia = login();
*/
